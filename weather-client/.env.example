# Asgardeo MCP Client App Configuration

# Authority URL (OIDC issuer base URL)
# For Asgardeo MCP Client Apps: https://api.asgardeo.io/t/{your-org-name}/oauth2/token
# Keep the /token suffix - it's required for proper endpoint discovery
VITE_WSO2_IS_URL=https://api.asgardeo.io/t/your-org-name/oauth2/token

# Client ID from your MCP Client App
# Get this from Asgardeo Console > Applications > Your MCP Client App > Info tab
VITE_WSO2_CLIENT_ID=your-client-id

# Weather MCP Server URL
# Use http://localhost:8080 for local development
# The Vite dev server proxies /mcp to this URL
VITE_MCP_SERVER_URL=http://localhost:8080

# CONFIGURATION STEPS FOR ASGARDEO MCP CLIENT APP:
#
# 1. Go to https://console.asgardeo.io
# 2. Create a new application:
#    - Click "New Application"
#    - Select "MCP Client Application" type (NOT Single-Page Application)
#    - Give it a name (e.g., "Weather Client")
# 3. In the application settings:
#    - Add Authorized redirect URLs: http://localhost:5173/callback
#    - Add Allowed origins: http://localhost:5173
#    - Ensure Authorization Code grant type is enabled
# 4. Copy the Client ID to VITE_WSO2_CLIENT_ID above
# 5. Note: MCP Client Apps use local logout (no server-side logout URL needed)
